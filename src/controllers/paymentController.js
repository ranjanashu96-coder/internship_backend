import { Payment, Student } from "../models/index.js"; import { asyncHandler } from "../utils/asyncHandler.js"; import { AppError, ok } from "../utils/response.js";
export const webhook=asyncHandler(async(req,res)=>{const {transaction_id,status,student_id,amount}=req.body;if(!transaction_id||!student_id)throw new AppError("transaction_id and student_id are required",422);const [payment]=await Payment.findOrCreate({where:{transaction_id},defaults:{student_id,amount,status:status==="success"?"success":"failed",gateway_payload:req.body}});if(status==="success"){await payment.update({status:"success",gateway_payload:req.body});await Student.update({payment_status:"paid",internship_status:"active"},{where:{id:student_id}});}ok(res,{transaction_id,payment_status:payment.status},"Webhook processed");});
